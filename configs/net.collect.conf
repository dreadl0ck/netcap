# NETCAP config for collect tool
# Generated by NETCAP v0.5.2
# You can regenerate an up to date default configuration with:
# 	$ net <tool> -gen-config > net.<tool>.conf

# specify an address and port to listen for incoming traffic
addr 127.0.0.1:1335

# read configuration from file at path
config 

# generate keypair
gen-keypair false

# set size for membuf
membuf-size 12582912

# path to the hex encoded server private key
privkey 

# print netcap package version and exit
version false

