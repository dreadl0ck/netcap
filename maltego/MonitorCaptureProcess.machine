machine("netcap.MonitorCaptureProcess",
        displayName:"Monitor Capture Process",
        author:"Philipp Mieden",
        description: "") {

        //A Timer Machine has a function like this, indicating how often it should run in seconds
        onTimer(30) {

            //Put the sequence of transforms to run in here
            run("netcap.ToLiveAuditRecords")

        }
}
